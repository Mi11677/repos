System.out.println("");  вывод на печать       Ещё есть метод сравнения .equal — он работает более гибко и предсказуемо,
                                               чем ==, потому что двойное равно может сравнивать только числа и строки.
String name = "Код";    объявляет переменную

// целое, 1 байт
byte b;

// целое, 2 байта
short s;

// целое, 4 байта
int x = 420;

// целое, 8 байт
long l;

// дробное, 4 байта
float fl = 4.3;

// дробное, 8 байт
double d,q;

// логический тип, true или false
boolean bol;